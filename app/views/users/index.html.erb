<% provide(:title, 'All users')%>

<h1>All users</h1>

<%= will_paginate %>
<ul class="users">
    <%@users.each do |user|%>
    <%= render 'users/user', user: user %>
    <%end%>
    <!-- This is an amazing short form of the above code. Rails will automatically know
    how to iterate and pass the necessary local variables -->
    <!-- <%= render @users%> -->
</ul>
<%= will_paginate %>